# Core UI
streamlit>=1.45.0

# LLM backends
openai>=1.30.0

# Vector store
chromadb>=0.5.0
# Note: sentence-transformers/PyTorch removed — embeddings use OpenAI API
# (text-embedding-3-small) to avoid 400-500 MB RAM overhead on Streamlit Cloud.
# ChromaDB's built-in ONNX embedder is used as a local fallback when no API key
# is present.
opentelemetry-exporter-otlp-proto-grpc>=1.25.0
opentelemetry-proto>=1.25.0

# Web scraping (used by job_scraper.py)
playwright>=1.44.0
beautifulsoup4>=4.12.0

# Data / utils
requests>=2.31.0
pandas>=2.0.0
tqdm>=4.66.0
